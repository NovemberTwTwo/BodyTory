name: Deploy GCP

on:
  push:
    branches:
      - main
      - dev

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GCE_INSTANCE: ${{ secrets.GCE_INSTANCE }}
  GCE_INSTANCE_ZONE: ${{ secrets.GCE_INSTANCE_ZONE }}
  GCR_AUTH: ${{ secrets.GCR_AUTH }}
  DOCKER_IMAGE_NAME: bodytory

jobs:
  setup-build-publish-deploy:
    name: Setup, Build, Publish, and Deploy
    runs-on: ubuntu-latest

    env:
      ACCESSKEYID: ${{ secrets.ACCESSKEYID }}
      AI_API_KEY: ${{ secrets.AI_API_KEY }}
      SECRETACCESSKET: ${{ secrets.SECRETACCESSKET }}
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      COOKIE_PASSWORD: ${{ secrets.COOKIE_PASSWORD }}
      MAIL_ID: ${{ secrets.MAIL_ID }}
      MAIL_PASSWORD: ${{ secrets.MAIL_PASSWORD }}
      NEXT_PUBLIC_NAVER_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_NAVER_CLIENT_ID }}
      NEXT_PUBLIC_NAVER_CLIENT_SECRET: ${{ secrets.NEXT_PUBLIC_NAVER_CLIENT_SECRET }}
      NEXT_PUBLIC_KAKAO_KEY: ${{ secrets.NEXT_PUBLIC_KAKAO_KEY }}
      REGION: ${{ secrets.REGION }}
      CF_ID: ${{ secrets.CF_ID }}
      CF_TOKEN: ${{ secrets.CF_TOKEN }}
      NEXT_PUBLIC_IMG_URL: ${{ secrets.NEXT_PUBLIC_IMG_URL }}

    steps:
      - name: Checkout
        uses: actions/checkout@main

      - uses: google-github-actions/setup-gcloud@v0
        with:
          service_account_key: ${{ secrets.SERVICE_ACCOUNT_KEY }}
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          export_default_credentials: true

      - run: |-
          gcloud --quiet auth configure-docker

      # - name: Build
      #   run: |-
      #     docker-compose up -d --build

      # - name: Docker list check
      #   run: |-
      #     docker images

      # - name: Docker tagging
      #   run: |-
      #     docker tag next gcr.io/$PROJECT_ID/next

      # - name: Docker tagging
      #   run: |-
      #     docker tag flask gcr.io/$PROJECT_ID/flask

      # - name: Docker push to GCR
      #   run: |-
      #     docker push gcr.io/$PROJECT_ID/next

      # - name: Docker push to GCR
      #   run: |-
      #     docker push gcr.io/$PROJECT_ID/flask

      - name: Connect GCE
        run: |-
          gcloud compute ssh "$GCE_INSTANCE" --zone "$GCE_INSTANCE_ZONE" \
          --command='gcloud auth configure-docker' \
          --command='docker pull gcr.io/$PROJECT_ID/next' \
          --command='docker pull gcr.io/$PROJECT_ID/flask' \
          --command='docker run -p 3000:3000/tcp -d gcr.io/$PROJECT_ID/next' \
          --command='docker run -p 8080:8080/tcp -d gcr.io/$PROJECT_ID/flask'

      # - name: Connect GCE
      #   run: |-
      #     gcloud compute ssh "$GCE_INSTANCE" --zone "$GCE_INSTANCE_ZONE" --command='docker pull gcr.io/$PROJECT_ID/next'

      # - name: Connect GCE
      #   run: |-
      #     gcloud compute ssh "$GCE_INSTANCE" --zone "$GCE_INSTANCE_ZONE" --command='docker pull gcr.io/$PROJECT_ID/flask'

      # - name: Connect GCE
      #   run: |-
      #     gcloud compute ssh "$GCE_INSTANCE" --zone "$GCE_INSTANCE_ZONE" --command='docker run -p 3000:3000/tcp -d gcr.io/$PROJECT_ID/next'

      # - name: Connect GCE
      #   run: |-
      #     gcloud compute ssh "$GCE_INSTANCE" --zone "$GCE_INSTANCE_ZONE" --command='docker run -p 8080:8080/tcp -d gcr.io/$PROJECT_ID/flask'

      # - name: Connect GCE
      #   run: |-
      #     docker pull gcr.io/$PROJECT_ID/next

      # - name: Connect GCE
      #   run: |-
      #     docker pull gcr.io/$PROJECT_ID/flask

      # - name: Connect GCE
      #   run: |-
      #     docker run -p 3000:3000/tcp -d gcr.io/$PROJECT_ID/next

      # - name: Connect GCE
      #   run: |-
      #     docker run -p 8080:8080/tcp -d gcr.io/$PROJECT_ID/flask

      # - name: Docker pull
      #   run: |-
      #     docker pull gcr.io/$PROJECT_ID/next

      # - name: Docker pull
      #   run: |-
      #     docker pull gcr.io/$PROJECT_ID/flask

      # - name: Docker run
      #   run: |-
      #     docker run gcr.io/$PROJECT_ID/next

      # - name: Docker run
      #   run: |-
      #     docker run gcr.io/$PROJECT_ID/flask
